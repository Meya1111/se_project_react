# React + Vite

## Backend Repository (Required for Review)

- **Repo (public):** https://github.com/Meya1111/se_project_express.git >/se_project_express

### Run the Full Stack Locally

**Backend**
1. `git clone https://github.com/Meya1111/se_project_express.git>/se_project_express`
2. `cd se_project_express`
3. `npm install`
4. Create a `.env` with:

5. `npm run dev` (or `npm start` if that’s your script)
6. Backend runs at **http://localhost:3001**

**Frontend**
1. In another terminal: `cd se_project_react`
2. `npm install`
3.(e.g. `VITE_API_BASE_URL=http://localhost:3001`)
4. `npm run dev`

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh
